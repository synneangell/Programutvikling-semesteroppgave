package org.openjfx.base;

public class GenererUnikID {

    public void genererID() {
        AlleArrangementer alleArrangementer = AlleArrangementer.getSingelton();

        /*
        int id = 0;
        for (int i = 0; i < alleArrangementer.getArrangementer().size(); i++) {
            if(alleArrangementer.getArrangementer().get(i).getArrangementID() == id){
                id++;
            }
        }

        return id;*/
        /*
        int id = 0;
        for(int i = 0; i<alleArrangementer.getArrangementer().size(); i++){
            alleArrangementer.getArrangementer().get(i).setArrangementID(id);
            id++;
        }*/
    }

}
